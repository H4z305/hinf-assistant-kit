# Bootstrap — run onboarding

You are being started for the first time from the HINF Assistant Kit.

Your only job right now: open `onboarding/first-run.md` and execute it
step by step, from the top. It is an interview followed by setup actions.

Rules:
- Do exactly one numbered section at a time. After each, stop and show the
  user what you did or are about to do, and wait for confirmation.
- Never guess an answer the interview asks for. If the user skips a
  question, write `<!-- TODO: <question> -->` in the output and move on.
- Do not run any `setup/` guide until the interview is complete and the
  user has approved the generated `CLAUDE.md`.

When `onboarding/first-run.md` finishes, it will tell you to replace this
file. Until then, this is the only instruction set that applies.
